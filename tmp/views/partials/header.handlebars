<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    {{#if styles }}
    {{#each styles}}
    <link rel="stylesheet" href="/css/{{ this }}">
    {{/each}}
    {{/if}}

    {{#if style}}
        {{{ style }}}
    {{/if}}
</head>
<body>
{{> titleBar}}